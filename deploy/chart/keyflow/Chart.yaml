apiVersion: v2
name: keyflow
description: >-
  keyflow.fasttrackstudio.app — the Keyflow site: the landing page, the
  live chart editor, and the guide. A static wasm bundle and nothing
  else; there is no server and no state.
type: application
home: https://github.com/FastTrackStudios/keyflow
sources:
  - https://github.com/FastTrackStudios/keyflow
# Chart version — bump on chart changes.
version: 0.1.0
# Tracks the workspace version ([workspace.package].version). The image
# tag defaults to this unless values override it, which in practice they
# always do — argocd-image-updater writes the digest it rolled.
appVersion: "0.1.0"
