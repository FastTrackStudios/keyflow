# Changelog

All notable changes to this project are documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/);
this project follows [Conventional Commits](https://www.conventionalcommits.org/en/v1.0.0/)
so the file is regenerated by `git cliff` from commit history.

## [Unreleased]

### Build

- *(nix)* Direnv + dev shell with dx, wasm-bindgen 0.2.122, wasm clang
- Optimize deps in dev profile to fix wasm 'too many locals'
- *(docs)* Fly.io deployment for keyflow.fasttrackstudio.app
- *(nix)* Point dioxus-flake at codeberg.org/FastTrackStudios
- Declare all sibling deps as codeberg git deps
- Unify fts-ui on codeberg, refresh keyflow.git lock entries
- Settle on dioxus-native 0.7.9 — defer the 0.8-alpha widget system
- *(nix)* Ship git in the ci shell — container git ABI-clashes with nix glibc

### CI

- *(docs)* Nix-based continuous deployment to fly.io
- *(docs)* Read fly token from FLY_DEPLOY_KEY org secret
- *(docs)* Standardize on FLY_API_TOKEN secret name
- *(docs)* Target codeberg-medium hosted runner
- *(docs)* Run on self-hosted THEBATTLESHIP runner
- *(docs)* Mirror deploy workflow into .forgejo/workflows
- *(docs)* Fully-qualify install-nix-action for forgejo resolution
- *(docs)* Trigger deploy on .forgejo workflow path too
- *(docs)* Clone sibling workspace repos before building
- *(docs)* Pin sibling clones to main branch
- *(docs)* Bridge Editor's ../FastTrackStudio/keyflow path-dep via symlink
- *(docs)* Assemble real workspace layout — cargo rejects symlinked dupes
- *(docs)* Drop workspace-assembly — repo is self-contained now
- *(docs)* Pin dodeca 0.6.1 — newer ddc breaks section.html template API
- Green gate — ignore-gate lineage-divergence tests, fix action refs
- Remove /homeless-shelter before nix builds in container
- Trigger — .#ci shell closure now on fasttrackstudio cachix
- Authenticate codeberg git fetches + cache cargo

### Chore

- Add color-palette to workspace dependencies
- *(engraver-proto)* Bump vello 0.6→0.7 and kurbo 0.12→0.13
- Restore in-progress working tree changes across all crates
- Update deps and fix mutex lock in action test
- Reorganize into crates/ workspace layout
- Bundle fonts into engraver-proto crate
- Add .gitignore and AGENTS.md
- Track Cargo.lock, remove from .gitignore
- Feeding the clippy monster
- Update Cargo.lock
- Stage pending changes from previous session
- Replace local path deps with GitHub git deps for sibling repos
- Add commented-out local path patch overrides for dev workflow
- Delete dead duplicate modules; surface dead code via unused=warn
- *(docs)* Remove dodeca/vite SSG scaffolding; keep content
- Repoint git deps from starcommand.live to Codeberg
- Repoint git deps from starcommand.live to Codeberg
- Update lock for codeberg git deps

### Documentation

- Add project README and VISION documents
- Add architecture, improvements, and melody pipeline documentation
- *(guide)* Add Rhythm page
- *(guide)* Add Melody page
- *(guide)* Melody octaves use ":", fix dropped-melody examples
- *(guide)* Melody notes default to a quarter; simplify examples
- *(guide)* Add Sections page
- *(guide)* Add Lyrics page (completes the guide)
- *(guide)* Document Pre-Chorus and section sub-labels
- *(guide)* Add Key & Meter Changes page
- *(guide)* Document the V/V secondary-dominant slash distinction
- *(handoff)* Record secondary dominants done + inversion plan
- *(guide)* Document ^ figured bass and inversions
- *(guide)* ^ inversions resolve; quotes for text annotations
- *(handoff)* Inversion edges tightened
- *(guide)* Round out Roman secondary-chord/inversion docs
- *(guide)* Add Annotations & Expression page
- *(guide)* Add Repeats & Endings page
- *(handoff)* Bring up to date
- Minimal README — the docsite carries the guide now

### Features

- Add keyflow, signal, and sync cell scaffolding
- Implement Keyflow protocol with chord chart system
- *(keyflow)* Integrate color-palette for section colors
- *(keyflow-proto)* Re-export Renderer from highlighting module
- *(keyflow-ui)* Add chart editor UI crate
- *(keyflow)* Enhance chart cursor, renderer, and editor UI
- *(desktop)* Integrate dock system, actions, and rig panels
- *(daw)* FX parameter improvements and discrete detection
- *(guide)* Add GuideService trait and guide event types to keyflow-proto
- *(guide)* Implement guide event generation in keyflow-midi
- *(chart-renderer)* Add LOD tiling, glyph merging, perf window, and AA mode selection
- *(engraver)* Add duration stretch formula and spring model for spacing
- *(engraver)* Wire spring justification into segment layout
- *(engraver)* Add spring-based measure width distribution
- *(engraver)* Improve chord symbol collision prevention with NON_KERNING semantics
- *(engraver)* Add squeeze algorithm for overflow handling
- *(engraver)* Enforce minimum measure width with duration scaling
- *(engraver)* Wire spacing improvements into all layout modes
- *(ui)* Add Messengers of Hope chart as default example
- Chord-syllable alignment, lyrics, ChordPro, and document model
- *(keyflow-ui)* Add WASM WebGL2 rendering backend for chart parity
- Staccato chords, stop signs, Vienna RPP import, and general MIDI improvements
- Pitched melody note rendering with dynamic system spacing
- Extract melody lines from RPP LINES track for INST/SOLO sections
- *(parser+engraver)* Floating slash-bass, suspension figures, single-lane join
- *(parser)* Rhythm-slash overrides /Duration default; section-header key change
- *(engraver+chart)* Key-sig changes, spacing, bass clef; finalize LGW chart
- *(engraver)* Split GPU renderer behind a wgpu feature; svg works GPU-free
- *(chord)* Contextual b<digit> resolution (note B vs flat degree)
- *(chord)* Optional `:` separator between degree and quality
- *(chord)* Bare number chords take the key's diatonic quality
- *(chord)* () rhythm grouping
- *(melody)* Octave via ":" instead of "()"
- *(melody)* Default to a quarter note when no duration is given
- *(chord)* Roman-numeral secondary dominants (V/V)
- *(chord)* ^ marks inline figured bass / inversions
- *(chord)* ^ inversions resolve to real inverted chords
- *(web-editor)* Unify vox to 0.8.2 (f31f1040) — wasm graph is mio-free
- *(web-editor)* Wire hover tooltips + resolved-chord overlay toggle
- *(ide)* Per-position chord resolution for hover + doc-absolute chord spans
- *(keyflow)* Bare number-system charts render with no metadata; default bass clef
- *(keyflow)* Section header source spans + skip label for implicit sections
- *(engraver)* ChartLayoutResult::content_bounds for snippet shrink-wrap
- *(engraver)* Render title header in continuous/snippet layout
- *(docs)* Keyflow docsite — kf-block preprocessor + shared fonts + guide charts
- *(docs)* Port vox/bearcove theme — Recursive font, sidebar TOC, dark mode
- *(docs)* Highlight kf-src fences with the editor's highlighter
- *(docs)* Session-view studio theme
- Upstream dioxus 0.7.9 + crates-io anyrender 0.10

### Fixes

- Repair workspace Cargo.toml, fix stale paths to workspace deps, fix include_bytes font paths, auto-fix warnings
- *(page-rendering)* Suppress subtitle placeholder when subtitle is absent
- *(chart)* Chord memory first-wins, breakdown label, hits suppression, and push chord in mixed measures
- *(engraver)* Use cumulative beat positions for chord min-width segments
- Stem alignment, beam direction, accidentals, and natural signs
- *(parser+cli)* Document routing, inline dyn/hairpin, comparator, 60Hz bench
- *(engraver)* Autoplace figured bass to clear chord symbols
- *(parser)* Tie slashes across barlines; figure-bar takes its slash count
- *(cli)* PDF export uses A4 paged layout matching PNG, full font set
- *(engraver)* Size suspension figures at 80% of the chord root size
- *(engraver)* Suspension figures hug their chord, use plain text font
- *(engraver)* Suspension figures — text font, and standalone vs attached layout
- *(engraver)* Anchor chord symbols by measure-local beat, not absolute position
- *(parser)* Attached suspension figures keep multi-part hyphenated figures
- *(chord)* Dim7 fully-diminished, accidental Roman roots, omit alias
- *(parser)* Keep leading flat lowercase for Roman roots in charts
- *(chord)* Preserve case of single lowercase Roman numerals
- *(sections)* Drop single-letter section abbreviations
- *(sections)* Pre-/post- headers and sub-labelled first sections
- *(chord)* Exact inversion bass + ignore chord memory
- *(parser)* Assign document-absolute chord source_spans
- *(engraver)* Render Nashville number-system chords as the number, not C{n}
- *(engraver)* Content_bounds must apply node transforms + cover text/glyphs
- *(engraver)* Scale Nashville/degree chord roots to letter cap height
- *(docs)* Charts render as flat full-width strips
- *(docs)* Dodeca v0.14.15 — subsections are objects now; pin CI to match
- Clear the workspace clippy gate (one red item remains)

### Other

- Improve MIDI chart import and CLI export progress
- Use explicit Keyflow section metadata in MIDI import
- Migrate session dep from FastTrackStudio.git to session.git

Session has been extracted into its own repo at
FastTrackStudios/session.
- Rename roam → vox across all crates

The bearcove/roam RPC framework has been renamed to vox.
Updates all dependency references, imports, and git URLs.
Pins vox to rev 53bff48.
- Enable local daw patch for development

Uncomments [patch] for daw.git to resolve local workspace.
- Update vox to Codys-Wright/vox at 2a2f793b
- Add DawModule implementation

Implement daw::DawModule trait for use in FTS-Extensions.
Exports module() -> Box<dyn DawModule> with actions and lifecycle.
- Ignore local docs artifacts
- Add ChordPro lyric layers to keyflow documents
- Add keyflow language tooling
- Add keyflow UI diagnostics and FTS chrome
- Refine engraver chart rendering pipeline
- Improve MIDI import tooling and fixtures
- Add project docs site and dataset fetch notes
- Add repeatable Nix dev shell
- Add derived lyric sync segments
- Add line-level lyric sync example
- Support plain ChordPro section headings
- Attach blank lyric tracks for chord-only sections
- Use interlude section in Build My Life example
- Remove beads issue tracking instructions
- Add capn hooks
- Fix capn install command in hooks
- Use local capn binary in hooks
- Bump facet to 0.46 and vox to 5ce8cdd9

Align with the rest of the FTS stack on facet 0.46 and the
upstream-synced vox fork (rev 5ce8cdd9). facet-format pinned to 0.47
to match its release line.
- Break daw cycle: depend on daw-module instead of daw facade

keyflow only used daw::module::{ActionDef, DawModule, ModuleContext},
which live in the leaf daw-module crate. Switching keyflow's lib to
daw-module breaks the daw → daw-reaper → keyflow → daw cycle exposed
after the facet 0.46 / vox 5ce8cdd9 bump.
- Sync keyflow to Forgejo main
- Merge pull request 'Sync keyflow to Forgejo main' (#1) from sync-keyflow-main into main
- Pull architect from git.starcommand.live (was local path)
- Merge pull request 'deps: pull architect from starcommand mirror (was local path)' (#2) from chore/architect-via-starcommand into main
- Migrate daw refs from github to git.starcommand.live
- Merge pull request 'deps: migrate daw refs from github to git.starcommand.live' (#3) from chore/daw-via-starcommand into main
- Fix highlighting import path (moved under text module)
- Merge pull request 'keyflow-ui: fix highlighting import path' (#4) from fix/highlighting-import-path into main
- Full git deps + adopt FTS hygiene template
- *(lotf)* Make chord/melody-split version canonical; update skill
- *(web-editor)* Wire keyflow to the Editor repo + vox 0.8.2 alignment
- Altered scale-degree roots (b3/#4); drop stale lord_of_the_fight tests
- Drop the {section} comment annotations from the seed chart
- Sync keyflow to Forgejo main
- Merge pull request 'Sync keyflow to Forgejo main' (#1) from sync-keyflow-main into main
- Pull architect from git.starcommand.live (was local path)
- Merge pull request 'deps: pull architect from starcommand mirror (was local path)' (#2) from chore/architect-via-starcommand into main
- Migrate daw refs from github to git.starcommand.live
- Merge pull request 'deps: migrate daw refs from github to git.starcommand.live' (#3) from chore/daw-via-starcommand into main
- Fix highlighting import path (moved under text module)
- Merge pull request 'keyflow-ui: fix highlighting import path' (#4) from fix/highlighting-import-path into main
- Merge codeberg/main (histories joined; tree kept from branch — codeberg lineage was a stale rebase of the same work, its one unique change reapplied next)
- Merge pull request 'Editor integration, keyflow docsite + theme, fly.io CD' (#1) from feat/editor-integration into main

### Performance

- *(ui)* Print explicit 60Hz budget verdict in headless_perf
- *(web-editor)* Debounce preview + inject engraving fonts once

### Refactor

- *(engraver)* Replace vello::Scene with anyrender PaintScene trait
- *(keyflow-ui)* PaintScene abstraction, remove vello scene cache
- *(ui)* Panel registration system and dock architecture
- *(signal)* Rename signal2 back to signal
- Apply crate facade pattern to keyflow workspace
- *(engraver)* Remove dead Score notation model; chart pipeline is the one model
- *(parser)* Extract phrase-repeat expansion from parse_chord_line_inner
- *(keyflow-proto)* Remove dead code
- *(keyflow-text)* Clear parser dead code
- *(keyflow-midi)* Clear dead code
- *(keyflow-musicxml)* Drop unused position_from_beat
- *(engraver-proto)* Remove dead code

### Style

- *(engraver)* Suspension figures slightly smaller and italic
- *(engraver)* Oblique suspension figures via shear; nudge smaller
- Sweep machine-applicable unused warnings (cargo fix)

### Tests

- Align tests with kept-lineage behavior (1958 → 1969 passing)


