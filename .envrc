# direnv config — auto-enters the nix dev shell when you `cd` into
# the repo (provided you have direnv + nix-direnv installed).
#
# nix-direnv: https://github.com/nix-community/nix-direnv
#   Install on NixOS: `programs.direnv.enable = true;` +
#                     `programs.direnv.nix-direnv.enable = true;`
#   On other systems: `nix profile install nixpkgs#nix-direnv` and
#                     follow the project's setup.sh.
#
# First time: `direnv allow` from the repo root.

if ! has nix_direnv_version || ! nix_direnv_version 3.0.4; then
  source_url "https://raw.githubusercontent.com/nix-community/nix-direnv/3.0.4/direnvrc" "sha256-DzlYZ33mWF/Gs8DDeyjr8mnVmQGx7ASYqA5WlxwvBG4="
fi

use flake

# A few quality-of-life env vars that aren't worth re-deriving every
# time `nix develop` runs. These mirror the shellHook in flake.nix so
# `direnv allow` users get the same setup without entering the shell
# explicitly.
export CARGO_TARGET_WASM32_UNKNOWN_UNKNOWN_RUNNER=wasm-bindgen-test-runner
: "${DATABASE_URL:=sqlite://./example.db?mode=rwc}"
export DATABASE_URL

# Watch additional files: re-evaluate the shell on config changes.
watch_file flake.nix flake.lock rust-toolchain.toml
